<Window x:Class="Task1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task1"
        mc:Ignorable="d"
        Title="Обробка Списку Дійсних Чисел" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button x:Name="LoadButton" 
                    Content="Завантажити список з файлу" 
                    Width="200" 
                    Margin="5" 
                    Padding="5"
                    Click="LoadButton_Click"/>
            <Button x:Name="ProcessSaveButton" 
                    Content="Обробити та Зберегти список" 
                    Width="200" 
                    Margin="5" 
                    Padding="5"
                    Click="ProcessSaveButton_Click"
                    IsEnabled="False"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Початковий Список" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="1" Text="Оброблений Список" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="OriginalListBox" 
                     Grid.Column="0" 
                     Margin="5"
                     BorderBrush="Gray"
                     BorderThickness="1"/>

            <ListBox x:Name="ProcessedListBox" 
                     Grid.Column="1" 
                     Margin="5"
                     BorderBrush="Gray"
                     BorderThickness="1"/>
        </Grid>

    </Grid>
</Window>
